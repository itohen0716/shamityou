// Ver.4.0 compatibility entry point.
// The tuning implementation now lives in tuning-play.js and uses ShianAudioEngine.
if (!window.ShianAudioEngine) console.warn("audio-engine.js must be loaded before tuning-app.js");
